import { Language } from "./language.interface";

export interface Hero{
  first: Language;
  name: Language;
  position: Language;
  resume: Language;
  callToAction: Language;
  imageUrl: string;
}

